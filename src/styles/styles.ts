import styled from "styled-components";

export const DashboadContainer = styled.div`
  flex-direction: column;
  height: 100vh;
  overflow-x: hidden;

  div {
    display: column;
    align-items: center;
  }

  .content {
    display: column;
    align-items: center;
    margin-left: 15rem;
    margin-top: -32rem;
  }
`;